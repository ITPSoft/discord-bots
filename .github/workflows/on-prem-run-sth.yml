name: Run-selfhosted
on:
  release:
    types: [created]
jobs:
  build:
    runs-on: greatlibrary-docker
    steps:
    - uses: actions/checkout@v1
    - name: build packages
      run: docker run -d ubuntu --name my-test
